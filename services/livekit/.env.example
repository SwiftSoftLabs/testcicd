# Copy to .env on the VPS (never commit .env).
# Generate keys: docker run --rm livekit/livekit-server generate-keys
LIVEKIT_API_KEY=
LIVEKIT_API_SECRET=

# Must match Caddyfile hostname (e.g. 5-78-232-172.sslip.io or DuckDNS).
LIVEKIT_TURN_DOMAIN=

# Generate: openssl rand -hex 24
REDIS_PASSWORD=
