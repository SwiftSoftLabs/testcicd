# Conflict lab

This file exists to practice resolving merge conflicts in Gitea pull requests.

## main version

- Keep Gitea as the backend engine for VC
- SQL migration comments should match production deploy notes
- Empty-state copy on version-control page is owned by main

## Resolution hint

When you see conflict markers, pick the wording you want or combine both sides, then remove `<<<<<<<`, `=======`, and `>>>>>>>`.
